function KeySequence() {
  this._X_ = Object.create(null);
}

KeySequence.prototype._M_ = function (key) {
  var lookup = this._X_;

  if (lookup[key]) {
    return key + "_" + lookup[key]++;
  }

  lookup[key] = 1;
  return key;
};

module.exports = KeySequence;