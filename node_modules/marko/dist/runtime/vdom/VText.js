var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");

function VText(value, ownerComponent) {
  this.bN_(-1 /* no children */, ownerComponent);
  this.bO_ = value;
}

VText.prototype = {
  ch_: true,

  bP_: 3,

  bM_: function (doc) {
    return doc.createTextNode(this.bO_);
  },

  __: function () {
    return new VText(this.bO_);
  }
};

inherit(VText, VNode);

module.exports = VText;