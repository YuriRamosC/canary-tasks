var domData = require("../components/dom-data");
var keysByDOMNode = domData.ai_;
var vElementByDOMNode = domData.ag_;
var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");
var createFragmentNode = require("./morphdom/fragment").ar_;

function VFragment(key, ownerComponent, preserve) {
  this.bN_(null /* childCount */, ownerComponent);
  this.bQ_ = key;
  this.bR_ = preserve;
}

VFragment.prototype = {
  bP_: 12,
  bM_: function () {
    var fragment = createFragmentNode();
    keysByDOMNode.set(fragment, this.bQ_);
    vElementByDOMNode.set(fragment, this);
    return fragment;
  }
};

inherit(VFragment, VNode);

module.exports = VFragment;